<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        tools:targetApi="24">

        <!-- main activity dlm app -->
        <activity
            android:name=".Register"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- main activity -->

        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- learn ui -->

        <activity android:name=".Learn_Radio_Checkbox"
            android:exported="true" />

        <activity
            android:name=".LearnButton_Text"
            android:exported="true" />

        <activity android:name=".LearnLayout"
            android:exported="true">
        </activity>

        <activity android:name=".LearnListView" android:exported="true"/>

        <activity android:name=".LearnSpinner" android:exported="true"/>

        <activity android:name=".LearnXML" android:exported="true"/>

        <activity android:name=".LearnXML_2" android:exported="true" />




    </application>

</manifest>